
							CLASSES
						
tkinter.Tk():
	Tk.update()
	Tk.update_idletasks()
	Tk.title('')
	Tk.wm_attributes('-topmost')								-объект, определяющий свойства окна
	
tkinter.Entry()
	.get()
	.insert(0, 'text')
	.delete(0, END)
	justify
tkinter.Label()
	font
	bd 		- граница(px)
tkinter.Button()
	text
	command
	bg
	fg
	activebackground
	activeforeground
	width
	height

			Methods
	.bind()	-	событие
	.pack()	-	менеджер геометрии
	.mainloop()-	главный цикл обработки событий
	.config()

		Методы геометрии
	.pack(side=, padx=, pady=, expand=, fill=, anchor=SNEW)
	.grid()
	.place()

tkinter.Text()
	.window_create()
tkinter.Scrollbar()

					Event
bind(<modifier-type-detail>)
event.char		возвращает символ нажатой клавиши


tkinter.PhotoImage(file='path')							-объект картинка

Canvas():
	Canvas(width=, height, bg=)
	Canvas.create_line(width, fill, arrow, dash,
		 				activefill, arrowshape)
	Canvas.create_rectangle(fill, outline, activedash,
							width)
	Canvas.create_oval()
	Canvas.create_polygon()
	Canvas.create_text (text, justify, font, anchor,
						fill)
	Canvas.create_arc (start, extent, style=ARC(CHORD),
						fill, outline, width)
	Canvas.create_image(anchor=, image=)
	Canvas.delete()
	Canvas.itemconfig()
	Canvas.coords(obj)									-возвращает координаты объекта
	Canvas.move()
	Canvas.focus_set()
	Canvas.tag_bind()
	Canvas.bind_all()
