ps ax								-processes
jobs								-список фоновых задач
&									-задача в фоноомй режиме
kill %2								-прекращает фоновую задачу №2
Ctrl-R								-search in previous commands
Ctrl-D
Ctrl-C								-SIGINT
Ctrl-\								-SIGQUIT
kill								-SIGTERM
kill -9								-SIGKILL
Ctrl-S								-pause output
Ctrl-Q								-continue output
Ctrl-Z								-pause current process
bg									-starts paused process in background
fg									-starts paused process in foreground
history
less								-prints on a separate page
ls -lF
ls -lR
Ctrl+U								-clears a row
Ctrl+W								-clears a word
>(>>)								-перенаправление вывода
<(<<)								-перенаправление ввода
wc file.txt							-считает кол-во строк, слов и символов (в файле)
echo								-виводить введені символи на екран (можна преренаправити у файл >)
cat									-зчитує текстовий файл та виводить на екран
man 'command'
info 'command'
'command' --help
apropos 'str'						-комманда поиска подходящей комманды
man -k 'str'						-комманда поиска подходящей комманды
touch								-создать файл
cp									-копировать папку или файл
mv									-переименовать или переместить папку или файл
rm									-удалить файл
rm -r								-видалити директорію із вмістом
mkdir								-створити директорію
rmdir								-видалити пусту директорію
ls -i								-просмотреть индексный дескриптор
ln									-создать жесткую ссылку
ln -s								-создать мягкую ссылку
chmod								-изменение прав доступа
sudo -s								-работа с правами администратора

tree								-отображает поддиректории в виде дерева
alias.<short> <command>				-назначает свою(короткую) комманду для комманды Bash

find -name
dpkg -i name.db						-установить локальный пакет с помощью менеджера dpkg
apt install ./name.db				-установить локальный пакет с помощью менеджера apt

fdisk -l							-показать разделы дисков
mkfs -t ext4 /dev/nvme0n1p4			-форматировать (файловая система, раздел)
mount -t ext4 /dev/nvme0n1p7 path	-монтировать раздел в точку монтирования(директорию)
df -h								-информация о текущей файловой системе
gcc -o <name.c> <name>				-компиляция файла с указанием имени скомпилированного файла
make <name> == gcc -o <name.c> <name>
export PATH=<path>:$PATH			-додати шлях до списку шляхів, з яких виконуються команди
echo $PATH							-вивести на екран список шляхів, з яких Bash виконує команди
vim .bashrc							-щоб зберегти шлях, відкрити файл .bashrc і записати і зберегти команду: export PATH=<path>:$PATH
vim .profile						-відкрити файл .profile і записати і зберегти команду: export PATH=<path>:$PATH
sudo vim /etc/profile				-додати шлях для всі користувачів - записати і зберегти команду: export PATH=<path>:$PATH

grep								-фільтр регулярних виразів
sed									-редактор регулярних виразів
grep -n								-номери рядків
grep -r								-рекурсивно
grep -c								-кількість входжень
grep -o								-виводить лише символи запиту(без рядків)
grep -E								-розширений regex




